import { StyleSheet } from 'react-native';

import colors from './colors';

export default StyleSheet.create({
  stack_navigator_header_text: { color: 'white', fontWeight: '700', fontSize: 22 },
  toolbar: { backgroundColor: colors.main_theme_green },
});
